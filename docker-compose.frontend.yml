services:
  frontend:
    build:
      context: ./frontend
    container_name: miniapp-frontend
    ports:
      - "3000:80"
    networks:
      - miniapp-network
    restart: unless-stopped

networks:
  miniapp-network:
    external: true
